name: CI/CD Flutter App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: flutter-actions/setup-flutter@v2
        with:
          channel: stable

      #- run: flutter analyze

      #- run: flutter test

      #- run: flutter test --flutter-widget-tester

      - name: Build Android App
        run: flutter build apk

      #- name: Build iOS App
      #  run: flutter build ios

# Add additional steps for deployment based on your needs

